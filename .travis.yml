language: python

install:
  - cd mysite
  - pip install -r requirements.txt

before_script:
  - python manage.py migrate

script:
  - black --check .
  - flake8 .
  - coverage run -m pytest
  - coverage report

after_script:
  - coveralls
